name: Ansible Deployment

on:
  workflow_run:
    workflows:
      - "Python CD - Containerize and publish image"
    branches:
      - main
      - master
    types:
      - completed
  push:
    branches:
      - test
  workflow_dispatch:

jobs:
  lint:
    name: Ansible Lint
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: solution/lab05/ansible
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install lint dependencies
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          pip install ansible-core ansible-lint
          for attempt in 1 2 3; do
            ansible-galaxy collection install -r collections/requirements.yml --timeout 120 && break
            if [ "$attempt" -eq 3 ]; then
              echo "ansible-galaxy collection install failed after 3 attempts"
              exit 1
            fi
            echo "Retrying collection install in 15s (attempt $((attempt + 1))/3)..."
            sleep 15
          done

      - name: Run ansible-lint
        run: ansible-lint playbooks/*.yml

  deploy:
    name: Deploy Web App
    needs: lint
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: self-hosted
    defaults:
      run:
        working-directory: solution/lab05/ansible
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install deployment dependencies
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          pip install ansible-core
          for attempt in 1 2 3; do
            ansible-galaxy collection install -r collections/requirements.yml --timeout 120 && break
            if [ "$attempt" -eq 3 ]; then
              echo "ansible-galaxy collection install failed after 3 attempts"
              exit 1
            fi
            echo "Retrying collection install in 15s (attempt $((attempt + 1))/3)..."
            sleep 15
          done

      - name: Configure SSH access
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          VM_HOST: ${{ secrets.VM_HOST }}
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H "$VM_HOST" >> ~/.ssh/known_hosts

      - name: Run deploy playbook
        env:
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
          VM_HOST: ${{ secrets.VM_HOST }}
          VM_USER: ${{ secrets.VM_USER }}
        run: |
          set -euo pipefail
          trap 'rm -f /tmp/vault_pass' EXIT
          printf '%s\n' "$ANSIBLE_VAULT_PASSWORD" > /tmp/vault_pass
          ansible-playbook playbooks/deploy.yml \
            -i inventory/hosts.ini \
            --vault-password-file /tmp/vault_pass \
            -e "ansible_host=$VM_HOST ansible_user=$VM_USER"

      - name: Verify deployment endpoint
        env:
          VM_HOST: ${{ secrets.VM_HOST }}
        run: |
          set -euo pipefail
          sleep 10
          curl -fsS "http://$VM_HOST:5000"
          curl -fsS "http://$VM_HOST:5000/health"
